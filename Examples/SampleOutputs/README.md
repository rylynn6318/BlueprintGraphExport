# Sample Outputs

This folder contains illustrative output examples for the plugin's exported artifacts.

- `SampleSummary.json` shows the normalized commandlet summary shape.
- `SampleBlueprint.md` shows the markdown layout for a Blueprint export.

These files are examples for onboarding and documentation. They are not authoritative test fixtures.
